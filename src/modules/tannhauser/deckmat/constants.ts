import * as Deck from '../deck';

export const deckImageVidth = 250;
export const height = Deck.Constants.maxDeckHeight + 300;
export const width = Deck.Constants.maxDeckWidth * 2 + deckImageVidth + 100;
