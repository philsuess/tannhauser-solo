import * as Deck from '../deck';

export const characterImageVidth = 250;
export const height = Deck.Constants.maxDeckHeight + 300;
export const width = Deck.Constants.maxDeckWidth * 2 + characterImageVidth + 100;
