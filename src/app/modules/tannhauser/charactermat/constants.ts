import * as Deck from '../deck';

export const character_image_width = 250;
export const height = Deck.Constants.maxDeckHeight + 300;
export const width = Deck.Constants.maxDeckWidth * 2 + character_image_width + 100;
